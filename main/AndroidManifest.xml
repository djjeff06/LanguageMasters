<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.msi.languagemasterapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CoverPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".QuizMenu" />
        <activity android:name=".QuizFlashCard" />
        <activity android:name=".QuizResult" />
        <activity android:name=".WordPhraseSelection" />
        <activity android:name=".LevelCategorySelection" />
        <activity android:name=".LevelSelection" />
        <activity android:name=".CategorySelection" />
        <activity android:name=".NameHighScore"></activity>
    </application>

</manifest>